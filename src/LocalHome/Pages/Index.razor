@page "/"
@inherits IndexBase
@using LocalHome.Components;
@using Microsoft.AspNetCore.Hosting;
@using System.IO;

<a href="AddLink">AddLink</a>
<Search OnFilter="DoFilter" />

@if (ULinks != null && ULinks.Count > 0)
{
    @foreach (var ul in ULinks)
    {
        <LinkGroup GroupVM="ul" />
    }
}
